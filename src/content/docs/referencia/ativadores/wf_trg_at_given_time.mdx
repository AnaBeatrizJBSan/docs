---
title: "ATIVADOR WIRED: No tempo escolhido"
description: Efeitos são ativados em uma hora definida depois do contador ser reiniciado.
sidebar:
  label: No tempo escolhido
  badge:
    text: NOVO
    variant: note
infobox:
  # == Infobox ==
  # hide: false # valor padrão
  title: No tempo escolhido
  # image: # url preenchida automaticamente

  # == Dados do Mobi ==
  revision: 50950
  classname: wf_trg_at_given_time
  name: No tempo escolhido
  description: Efeitos são ativados em uma hora definida depois do contador ser reiniciado.
  # product_name: # caso o nome na loja seja diferente do nome do Mobi
  # image_direction: 0 # valor padrão
  # image_not_animated: false # valor padrão
  # image_animated_state: 100 # valor padrão
  # icon: # url preenchida automaticamente
  availability: No catálogo e CA
  price: 6
  release_date: 2010-11-04

  # == Dados do Wired ==
  type: Ativador
  # requires_bot: false # valor padrão
  # requires_furni: false # valor padrão
  # requires_antena: false # valor padrão
  # requires_contract: false # valor padrão
  # requires_chest: false # valor padrão
  # execution_limit: 100 # valor padrão
  # execution_limit_per_user: 100 # valor padrão

  # == Dados do Ativador ==
  additional_sources: []
  # - Bots especificados pelo nome
  # - Habbo especificado pelo nome
  # - Usuário alcançado
  # - Usuário clicado
  # - Usuário acionador
  # - Item de ativação
---

import AccessibleVideo from "@components/AccessibleVideo.astro";
import Infobox from "@components/Infobox.astro"
import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';
import PseudoWiredCodeBlock from "@components/PseudoWiredCodeBlock.astro";
import wiredTimerNoWiredCreatorTools from "@assets/wf_trg_at_given_time/1-wired_timer-no-wired-creator-tools.mp4";
import WiredTimerCaptionsUrl from "@assets/wf_trg_at_given_time/1-wired_timer-no-wired-creator-tools-captions.vtt?url";
import WiredTimerDescriptionsUrl from "@assets/wf_trg_at_given_time/1-wired_timer-no-wired-creator-tools-descriptions.vtt?url";
import WiredTimerCaptionsRaw from "@assets/wf_trg_at_given_time/1-wired_timer-no-wired-creator-tools-captions.vtt?raw";
import WiredTimerDescriptionsRaw from "@assets/wf_trg_at_given_time/1-wired_timer-no-wired-creator-tools-descriptions.vtt?raw";
import exemploPraticoWiredNoTempoEscolhido from "@assets/wf_trg_at_given_time/3-exemplo-pratico.mp4";
import ExemploPraticoCaptionsUrl from "@assets/wf_trg_at_given_time/3-exemplo-pratico-captions.vtt?url";
import ExemploPraticoDescriptionsUrl from "@assets/wf_trg_at_given_time/3-exemplo-pratico-descriptions.vtt?url";
import ExemploPraticoCaptionsRaw from "@assets/wf_trg_at_given_time/3-exemplo-pratico-captions.vtt?raw";
import ExemploPraticoDescriptionsRaw from "@assets/wf_trg_at_given_time/3-exemplo-pratico-descriptions.vtt?raw";

O **Ativador Wired: No tempo escolhido** é responsável por executar uma pilha Wired quando o tempo interno do quarto atingir um valor específico.

<Infobox />

Antes de utilizá-lo, é importante entender qual é esse "tempo" citado no nome do Wired.

Esse tempo corresponde ao valor da variável global interna do jogo chamada **`@wired_timer`**. Ela é atualizada automaticamente a cada **0,5 segundo** (meio segundo), aumentando seu valor continuamente enquanto o quarto estiver ativo.

É possível acompanhar o valor atual dessa variável utilizando o comando **`:wired`** dentro do jogo e acessando a aba **Variáveis Globais**, como mostrado no vídeo abaixo:

<AccessibleVideo
  id="consulta-wired-timer"
  src={wiredTimerNoWiredCreatorTools}
  title="Consulta da variável global @wired_timer"
  captionsSrc={WiredTimerCaptionsUrl}
  captionsContent={WiredTimerCaptionsRaw}
  descriptionsSrc={WiredTimerDescriptionsUrl}
  descriptionsContent={WiredTimerDescriptionsRaw}
  width="55%"
/>

## Como escolher o tempo?

O campo de configuração do Wired No Tempo Escolhido permite definir o tempo em intervalos de 0,5, e esse valor será comparado com o valor atual da variável **`@wired_timer`**.

![Janela de configuração do Ativador Wired No tempo escolhido, com o campo Configurar tempo preenchido com 10](@assets/wf_trg_at_given_time/2-wired-no-tempo-escolhido.png "Janela de configuração do Ativador Wired No tempo escolhido, com o campo Configurar tempo preenchido com 10")

Como a variável aumenta uma unidade a cada 0,5 segundo, basta multiplicar o tempo desejado em segundos por **2** para descobrir qual será o valor da variável naquele momento.

**Exemplos:**

| Tempo desejado | Valor da variável `@wired_timer` |
| :-------------- | :-------------------------------: |
| 1 segundo      |                                2 |
| 5 segundos     |                               10 |
| 10 segundos    |                               20 |
| 30 segundos    |                               60 |
| 1 minuto       |                              120 |

## Exemplo prático

Neste exemplo, um **Piso Colorido** mudará de estado 10 segundos após um usuário pisar nele.

<AccessibleVideo
  id="exemplo-pratico-no-tempo-escolhido"
  src={exemploPraticoWiredNoTempoEscolhido}
  title="Exemplo prático do Ativador Wired No tempo escolhido"
  captionsSrc={ExemploPraticoCaptionsUrl}
  captionsContent={ExemploPraticoCaptionsRaw}
  descriptionsSrc={ExemploPraticoDescriptionsUrl}
  descriptionsContent={ExemploPraticoDescriptionsRaw}
/>

<Tabs>
  <TabItem label="Configuração">
    Primeira pilha
    <Steps>
    1. **Ativador:** No Tempo escolhido
        1. Indique o valor 10
    2. **Efeito:** Mudar estado do mobi
        1. Selecione o Piso Colorido
    </Steps>
    Segunda pilha
    <Steps>
    1. **Ativador:** Habbo anda no mobi
        1. Selecione o Piso Colorido
    2. **Efeito:** Reiniciar tempo
    </Steps>
  </TabItem>
  
  <TabItem label="PseudoWired">
    Primeira pilha
    <PseudoWiredCodeBlock code={`
    QUANDO
      - No tempo escolhido: 10
    ENTÃO
      - Mudar estado do Mobi: Piso Colorido
    `} />
    Segunda pilha
    <PseudoWiredCodeBlock code={`
    QUANDO
      - Habbo anda no Mobi: Piso Colorido
    ENTÃO
      - Reiniciar tempo
    `} />
  </TabItem>
</Tabs>

### O que acontece nesse exemplo?

Ao pisar no Piso Colorido, o **Efeito Wired: Reiniciar Tempo** redefine o valor da variável **`@wired_timer`** para **0**.

A partir desse momento, a variável volta a aumentar automaticamente em intervalos de 0,5 segundo.

Embora o **Ativador Wired: No tempo escolhido** esteja configurado com o valor **10**, é importante lembrar que cada unidade da variável representa apenas meio segundo. Portanto, quando a variável atingir o valor **20**, terão se passado **10 segundos**, momento em que o Piso Colorido mudará de estado.

## Como reiniciar o tempo?

Existem diferentes maneiras de reiniciar o valor da variável **`@wired_timer`**.

A mais comum é utilizando o **Wired Efeito: Reiniciar Tempo**, permitindo reiniciar a contagem exatamente no momento desejado.

Outra possibilidade é deixar o quarto completamente vazio durante aproximadamente **30 segundos**. Ao entrar novamente no quarto, a variável **`@wired_timer`** será reiniciada automaticamente.
